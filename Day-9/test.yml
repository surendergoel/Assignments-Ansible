---
 - name:
   hosts: 192.168.33.30
   gather_facts: true
   tasks:
    - name: List of all open ports
      shell: netstat -l
      register: results

    - name: See what netstat returned
      debug: var=results
...
